# Local settings for docker-compose
# Copy this file to docker-compose.override.yml and configure for your server
# This file is NOT committed to repository (added to .gitignore)
#
# Usage:
#   1. Copy: cp docker-compose.override.yml.example docker-compose.override.yml
#   2. Configure additional parameters (environment, volumes, etc.)
#
# Note: docker-compose automatically applies this file when working with docker-compose.yml
#
# ⚠️ IMPORTANT: Resources (cpus, memory) configured via ~/.dc_config:
#   - Use command: dc resources set [service] --cpus 2 --memory 2G
#   - Or edit ~/.dc_config manually
#   - Resources automatically generated to this file, don't edit manually!

services:
  # Example: additional environment variables
  # app:
  #   environment:
  #     - DEBUG=true
  #     - LOG_LEVEL=debug
  
  # Example: additional volumes
  # app:
  #   volumes:
  #     - /host/path:/container/path:ro
  
  # Example: settings for additional services (e.g., PostgreSQL)
  # postgres:
  #   image: postgres:15
  #   environment:
  #     - POSTGRES_DB=mydb
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '1'
  #         memory: '1G'
  
  # Example: volumes for additional services
  # volumes:
  #   postgres_data:

