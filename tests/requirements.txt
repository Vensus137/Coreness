# Тестовые зависимости для проекта
# Включает основные зависимости + тестовые инструменты
# НЕ устанавливаются на сервере, только для разработки и CI/CD

# Основные зависимости проекта (нужны для работы тестов)
-r ../requirements.txt

# Pytest и расширения
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

