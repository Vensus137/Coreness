# Test dependencies for the project
# Includes core dependencies + testing tools
# NOT installed on the production server, only for development and CI/CD

# Core project dependencies (required for running tests)
-r ../requirements.txt

# Pytest and extensions
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

